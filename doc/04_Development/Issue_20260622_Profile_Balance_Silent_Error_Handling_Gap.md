# Issue: 余额页汇总加载链路仍存在静默失败

日期：2026-06-22

## 现象

- `frontend-web/src/pages/profile/balance/index.vue` 对余额汇总加载仍在使用空 `catch`
- 请求失败时页面静默回退到默认值，用户无法判断是加载失败还是余额本身为 0

## 风险

- 余额页核心财务信息静默失败会误导用户判断账户状态。
- 后续扩展余额明细和更多财务指标时，静默失败分支容易继续扩散。

## 处理建议

1. 为余额汇总加载补充明确失败提示
2. 保持默认值兜底，避免渲染中断
3. 保持现有余额展示行为不变
