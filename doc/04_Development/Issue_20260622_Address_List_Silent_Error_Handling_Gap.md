# Issue: 地址列表页仍存在静默失败

日期：2026-06-22

## 现象

- `frontend-web/src/pages/address/list/index.vue` 的地址加载失败仅回退为空列表，没有提示
- 删除地址分支把弹窗取消与接口失败都落在空 `catch` 中

## 风险

- 用户无法区分“真的没有地址”和“地址加载失败”。
- 删除地址失败时页面静默，容易误以为操作已经完成。

## 处理建议

1. 为地址列表加载失败补充明确提示
2. 区分删除弹窗取消与接口失败
3. 保持原有空态、删除成功提示与刷新行为不变
