# Issue: 个人中心首页数据加载链路仍存在静默失败

日期：2026-06-22

## 现象

- `frontend-web/src/pages/profile/index.vue` 对个人统计和余额加载仍在使用空 `catch`
- 请求失败时页面静默回退到默认值，用户无法区分“真实为 0”还是“加载失败”

## 风险

- 个人中心核心数据静默失败会误导用户判断账户状态。
- 后续扩展账户概览指标时，静默失败分支容易继续扩散。

## 处理建议

1. 为个人统计和余额加载补充明确失败提示
2. 保持页面默认值兜底，避免渲染中断
3. 保持现有入口、展示和跳转行为不变
